type Query {
    authors: [Author]
    authorById(id: ID!): Author
    person(id:Int!): Person
}

type Author {
    id: ID!
    name: String!
    bookList: [Book]
}

type Book {
    id: ID!
    title: String!
    publisher: String
}